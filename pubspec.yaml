name: dss_lets
description: DSS Lets Housing Rental Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.0.0
  firebase_storage: ^12.0.0
  go_router: ^14.0.0
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.5
  image_picker: ^1.0.7
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
